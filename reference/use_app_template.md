# Scaffold shiny app in current working directory

This function will create files and folders in the user's current
working directory...

## Usage

``` r
use_app_template(template = "bslib_top_modules")
```

## Arguments

- template:

  One of vanilla_shiny, shiny_dashboard_modules, bslib_top_modules,
  module_template
